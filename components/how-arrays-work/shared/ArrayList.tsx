import { styled } from '@/stitches'

const ArrayList = styled('ul', {
  display: 'flex',
  justifyContent: 'center',
  flexWrap: 'wrap',
  gap: '12px',
  position: 'relative',
  zIndex: 1,
})

export default ArrayList
